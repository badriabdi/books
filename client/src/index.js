import React from "react";
import ReactDOM from "react-dom";//we should put all information in the passage so will
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
